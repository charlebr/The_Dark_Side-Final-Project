#include"func.cpp"
#include"gtest/gtest.h"
#include<cmath>

//This was the test to see if the area would generate.
struct square {
        __host__ __device__ float operator()(float x){return x*x};
};



//Was attempting to unit test the random float but there was error.


__host__ __device__ float operator()(){

if (sign ==0){
                return -(float)(rand() % (modulus_factor-1))+(float)((rand()%1000)/1000);
} else {

        return (float)(rand()%(modulus_factor-1))+(float)((rand()% 1000/1000);
};
}


//The simplest unit test but still got compiler errors

template<typename T>
T generate_values(int size, int range, int sign){
}
~
~
